node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Updater signing keys. The private key must never be committed; losing it
# means no future build can be verified as an update to this one.
*.key
*.key.pub
.sentry-keys/
