{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum surface the SENTRY window needs. Tauri denies everything not listed here. SENTRY's own #[tauri::command] verbs are the real API - they are deliberately narrow and need no entry in this file; what is listed below is only the framework surface the UI shell uses: events, the window, the tray menu and desktop notifications. Nothing here grants filesystem, shell, HTTP or process access.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "core:resources:default",
    "core:menu:default",
    "core:tray:default",
    "log:default",
    "notification:default",
    "updater:default"
  ]
}
